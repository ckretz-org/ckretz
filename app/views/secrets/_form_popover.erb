<dialog open id="secrets_form">
  <div class="bg-white p-4 rounded-lg w-full max-w-md">
    <div class="text-center">
      <h2 class="text-gray-500 grow text-lg font-bold inline-block"><%= secret.persisted? ? "Edit" : "New" %></h2>
    </div>
    <%= render partial: "secrets/form", locals: { secret: secret } %>
  </div>
</dialog>
